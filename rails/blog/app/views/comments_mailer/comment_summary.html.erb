<p>Hello <%= @owner.full_name %>,</p>
<p>Here are todays comments for your posts.</p>
<% @owner.posts.each do |p| %>
  <% post_comments = @all_comments_today.where(post: p) %>
  <% if post_comments.present? %>
    <p>Post title: <%= p.title %></p>
    <ul>
    <% post_comments.each do |comment| %>
        <li><%= comment.user.full_name %>: <%= comment.body %></li>
    <% end %>
    </ul>
  <% end %>
<% end %>
<p>Regards,</p>
<p>Blog Team</p>
