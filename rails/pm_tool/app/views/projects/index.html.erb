<h1>All Projects</h1>
<%= link_to("New Project", new_project_path, class: "btn btn-primary") %>
<div>
  <% @projects.each do |p| %>
    <div class="well">
        <%= p.title %>
        <%= link_to "Show", project_path(p) %>
    </div>
  <% end %>
</div>
